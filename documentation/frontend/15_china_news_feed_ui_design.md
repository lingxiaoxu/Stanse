# 15. China News Feed UI Design

## 概述

在 Feed 界面下方展示 `news_stanseradar_china` collection 的最新记录，仅在用户选择中文语言时显示。

## 设计原则

1. **信息密度平衡**：在有限空间内展示最多有价值信息
2. **渐进式展开**：默认显示摘要，用户可选择性展开详情
3. **视觉层次清晰**：使用卡片、折叠、标签页等组件区分信息层级
4. **符合现有风格**：使用 PixelCard、PixelButton 等现有组件
5. **性能优化**：虚拟滚动、懒加载等技术

## 数据结构分析

基于 `2026-01-22_09-01` 记录：
- **总新闻量**：53 条热榜新闻（分 10 组）
- **RSS 订阅**：3 条 (Hacker News)
- **统计信息**：11 个平台，成功率 100%
- **文档大小**：~170KB

**设计挑战**：
- ❌ 不能直接展示所有 53 条新闻（太长）
- ❌ 不能忽略重要信息（RSS、分组）
- ✅ 需要智能筛选和分层展示

---

## UI 设计方案（推荐）

### 整体布局

```
┌─────────────────────────────────────────────────────────────────┐
│                        Feed View                                 │
│  (现有的 Feed 内容)                                               │
└─────────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────────┐
│  🇨🇳 今日中文新闻速报                         [2026-01-22 09:01]  │
│  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━  │
│                                                                   │
│  📊 今日概览                                                      │
│  • 11 个平台  • 53 条热榜新闻  • 3 条 RSS 科技订阅              │
│                                                                   │
│  ▼ 🔥 热门新闻 Top 10                                            │
│  ▼ 📡 RSS 科技订阅 (3)                                           │
│  ▼ 📰 更多新闻分组 (8 组)                                        │
│                                                                   │
└─────────────────────────────────────────────────────────────────┘
```

---

## 第一层：概览卡片（默认可见，不可折叠）

### 组件结构

```typescript
<PixelCard className="china-news-overview">
  {/* Header */}
  <div className="header">
    <div className="title">
      🇨🇳 今日中文新闻速报
    </div>
    <div className="timestamp">
      {data.time.beijing_time} · {data.metadata.doc_id}
    </div>
    <div className="source-badge">
      StanseRadar China
    </div>
  </div>

  {/* Statistics */}
  <div className="statistics-grid">
    <StatCard
      icon="🏢"
      label="平台"
      value={data.statistics.platforms.success}
      total={data.statistics.platforms.total}
    />
    <StatCard
      icon="🔥"
      label="热榜新闻"
      value={data.statistics.hotlist.total}
    />
    <StatCard
      icon="📡"
      label="RSS 订阅"
      value={data.statistics.rss.matched}
    />
  </div>

  {/* Expandable Sections */}
  <CollapsibleSections />
</PixelCard>
```

### 视觉效果

```
┌─────────────────────────────────────────────────────────────────┐
│ 🇨🇳 今日中文新闻速报                        2026-01-22 09:01:24  │
│                                          [StanseRadar China]    │
│ ─────────────────────────────────────────────────────────────── │
│                                                                   │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐                      │
│  │   🏢     │  │   🔥     │  │   📡     │                      │
│  │ 平台      │  │ 热榜新闻  │  │ RSS订阅  │                      │
│  │ 11/11    │  │   53     │  │    3     │                      │
│  └──────────┘  └──────────┘  └──────────┘                      │
│                                                                   │
└─────────────────────────────────────────────────────────────────┘
```

**尺寸**: 高度约 180px

---

## 第二层A：热门新闻 Top 10（可折叠）

### 数据处理逻辑

```typescript
// 从所有 keyword_groups 中提取新闻
const allNews = data.hotlist_news.keyword_groups
  .flatMap(group => group.news_items)
  .sort((a, b) => a.rank - b.rank)  // 按排名排序
  .slice(0, 10);  // 取前10条
```

### UI 布局

```
▼ 🔥 热门新闻 Top 10                                    [展开/折叠]
┌─────────────────────────────────────────────────────────────────┐
│                                                                   │
│  #2  美国贸易代表：想和中国再谈谈，但不谈稀土                    │
│      📰 凤凰网 · 07:01                                 [rank-hot] │
│  ─────────────────────────────────────────────────────────────  │
│  #4  英国批准中国在伦敦新建使馆，凤凰记者实地探访                │
│      📰 凤凰网 · 07:01                                 [rank-hot] │
│  ─────────────────────────────────────────────────────────────  │
│  #4  中国第二个5万亿城市诞生                                     │
│      📰 头条 · 07:01                                   [rank-hot] │
│  ─────────────────────────────────────────────────────────────  │
│  #5  U23亚洲杯历史性晋级决赛：胜利对中国足球真的很重要           │
│      📰 澎湃新闻 · 07:01                               [rank-hot] │
│  ─────────────────────────────────────────────────────────────  │
│  #6  中国 GDP 首破 140 万亿增速 5%，140 万亿意味着什么？         │
│      📰 知乎 · 07:01                               [rank-normal] │
│                                                                   │
│  ... (5 more items)                                               │
│                                                                   │
│  [查看全部 53 条新闻 →]                                          │
│                                                                   │
└─────────────────────────────────────────────────────────────────┘
```

### 组件代码

```typescript
<CollapsibleSection
  title="🔥 热门新闻 Top 10"
  count={10}
  defaultExpanded={true}
>
  <div className="news-list">
    {topNews.map((news, idx) => (
      <NewsItem
        key={idx}
        rank={news.rank}
        rankDisplay={news.rank_display}
        rankClass={news.rank_class}
        title={news.title}
        url={news.url}
        platform={getPlatformName(news.url)}
        time={news.last_crawl_time}
      />
    ))}
  </div>

  <PixelButton
    variant="secondary"
    onClick={() => showAllNews()}
  >
    查看全部 {data.statistics.hotlist.total} 条新闻 →
  </PixelButton>
</CollapsibleSection>
```

**尺寸**: 折叠时 ~50px，展开时 ~600px

---

## 第二层B：RSS 科技订阅（可折叠）

### 数据处理

```typescript
// 按分类分组 RSS 新闻（从 html_content 或 rss_feeds 提取）
const rssCategories = [
  {
    name: "AI 相关",
    items: [/* ... */]
  },
  {
    name: "OpenAI / Claude",
    items: [/* ... */]
  },
  {
    name: "微软 / 谷歌 / 苹果",
    items: [/* ... */]
  }
];
```

### UI 布局

```
▼ 📡 RSS 科技订阅 (3)                                   [展开/折叠]
┌─────────────────────────────────────────────────────────────────┐
│                                                                   │
│  【微软 / 谷歌 / 苹果】                                           │
│  ─────────────────────────────────────────────────────────────  │
│  🕐 01-22 01:28  •  Hacker News                                 │
│  Waiting for dawn in search: Search index, Google rulings        │
│  and impact on Kagi                                              │
│  🔗 blog.kagi.com                                      [外部链接] │
│                                                                   │
│  【OpenAI / Claude】                                              │
│  ─────────────────────────────────────────────────────────────  │
│  🕐 01-22 00:04  •  Hacker News                                 │
│  Claude's new constitution                                       │
│  🔗 anthropic.com                                      [外部链接] │
│                                                                   │
│  【AI 相关】                                                      │
│  ─────────────────────────────────────────────────────────────  │
│  🕐 01-22 01:00  •  Hacker News                                 │
│  Autonomous (YC F25) is hiring – AI-native financial advisor     │
│  🔗 atg.science                                        [外部链接] │
│                                                                   │
└─────────────────────────────────────────────────────────────────┘
```

**尺寸**: 折叠时 ~50px，展开时 ~400px

---

## 第二层C：更多新闻分组（可折叠）

### 数据处理

```typescript
// 显示除了 Top 10 之外的其他分组
const otherGroups = data.hotlist_news.keyword_groups
  .filter(group => group.news_items.length > 0)
  .map(group => ({
    keyword: group.keyword || "综合热榜",
    count: group.news_items.length,
    topItems: group.news_items.slice(0, 3)  // 每组只显示前3条
  }));
```

### UI 布局

```
▼ 📰 更多新闻分组 (8 组)                                [展开/折叠]
┌─────────────────────────────────────────────────────────────────┐
│                                                                   │
│  ▶ 综合热榜 #2 (14 条)                                           │
│                                                                   │
│  ▶ 综合热榜 #3 (8 条)                                            │
│                                                                   │
│  ▶ 财经新闻 (5 条)                                               │
│                                                                   │
│  ▶ 科技动态 (3 条)                                               │
│                                                                   │
│  ... (4 more groups)                                              │
│                                                                   │
└─────────────────────────────────────────────────────────────────┘
```

展开某个分组：

```
▼ 综合热榜 #2 (14 条)
┌─────────────────────────────────────────────────────────────────┐
│  #1  日债风暴叠加格陵兰危机，"抛售美国"重现                      │
│      📰 来源 · 07:01                                   [rank-hot] │
│  ─────────────────────────────────────────────────────────────  │
│  #2  加拿大总理卡尼重磅演讲：基于规则的秩序已死                  │
│      📰 来源 · 07:01                                   [rank-hot] │
│  ─────────────────────────────────────────────────────────────  │
│  #3  美国赢学升级,懂王天天开赢趴                                 │
│      📰 来源 · 07:01                                   [rank-hot] │
│                                                                   │
│  [显示更多 11 条 →]                                              │
└─────────────────────────────────────────────────────────────────┘
```

**尺寸**: 折叠时 ~50px，全部展开时 ~800px

---

## 完整组件层级结构

```typescript
<ChinaNewsSection>  // 仅在语言=中文时渲染
  {/* 第一层：概览 */}
  <OverviewCard>
    <Header />
    <StatisticsGrid />
  </OverviewCard>

  {/* 第二层：可折叠部分 */}
  <ExpandableSections>
    {/* A: 热门新闻 */}
    <CollapsibleSection title="🔥 热门新闻 Top 10">
      <NewsList items={topNews} />
      <ViewAllButton />
    </CollapsibleSection>

    {/* B: RSS 订阅 */}
    <CollapsibleSection title="📡 RSS 科技订阅">
      <RSSCategories categories={rssCategories} />
    </CollapsibleSection>

    {/* C: 更多分组 */}
    <CollapsibleSection title="📰 更多新闻分组">
      <NestedCollapsibleList groups={otherGroups} />
    </CollapsibleSection>
  </ExpandableSections>
</ChinaNewsSection>
```

---

## 空间占用估算

| 状态 | 高度 | 说明 |
|------|------|------|
| 全部折叠 | ~330px | 概览(180px) + 3个折叠标题(50px × 3) |
| 仅展开热门新闻 | ~930px | 概览 + 热门(600px) + 2个折叠 |
| 全部展开 | ~2130px | 概览 + 热门(600px) + RSS(400px) + 分组(800px) + 折叠标题 |

**结论**：默认折叠状态下仅占用 ~330px，对 Feed 影响最小。

---

## 技术实现要点

### 1. Schedule Function (Firebase Functions)

```typescript
// functions/src/chinaNewsListener.ts
export const onChinaNewsCreate = onDocumentCreated(
  "news_stanseradar_china/{docId}",
  async (event) => {
    const docId = event.params.docId;

    // 触发通知或更新 latest_china_news 缓存文档
    await admin.firestore()
      .collection("cache")
      .doc("latest_china_news")
      .set({
        docId,
        timestamp: admin.firestore.FieldValue.serverTimestamp(),
        data: event.data?.data()
      });
  }
);
```

### 2. 前端数据获取

```typescript
// services/chinaNewsService.ts
export async function getLatestChinaNews() {
  const db = getFirestore(app);

  // 方案1：从缓存获取
  const cacheDoc = await getDoc(doc(db, 'cache', 'latest_china_news'));

  // 方案2：直接查询最新文档
  const q = query(
    collection(db, 'news_stanseradar_china'),
    orderBy('__name__', 'desc'),
    limit(1)
  );
  const snapshot = await getDocs(q);

  return snapshot.docs[0]?.data();
}
```

### 3. 条件渲染

```typescript
// components/views/FeedView.tsx
const { language } = useLanguage();

return (
  <div className="feed-container">
    {/* 现有 Feed 内容 */}
    <Feed />

    {/* 中文新闻部分 - 仅在中文时显示 */}
    {language === Language.ZH && (
      <ChinaNewsSection />
    )}
  </div>
);
```

### 4. 性能优化

```typescript
// 使用 React.memo 避免不必要的重渲染
export const ChinaNewsSection = React.memo(() => {
  const [data, setData] = useState(null);
  const [expanded, setExpanded] = useState({
    hotNews: true,
    rss: false,
    moreGroups: false
  });

  // 懒加载：仅在展开时渲染内容
  // 虚拟滚动：长列表使用 react-window
  // 图片懒加载：使用 Intersection Observer
});
```

---

## UI 样式指南

### 颜色方案（符合现有风格）

```css
.china-news-section {
  --china-accent: #dc2626;      /* 中国红 */
  --china-accent-light: #fca5a5;
  --tech-blue: #3b82f6;         /* RSS科技订阅 */
  --hot-orange: #f97316;        /* 热门标识 */
}
```

### 组件样式

```scss
.china-news-overview {
  border-left: 4px solid var(--china-accent);
  margin-top: 2rem;

  .header {
    display: flex;
    align-items: center;
    justify-content: space-between;

    .title {
      font-size: 1.25rem;
      font-weight: 700;
      color: var(--china-accent);
    }

    .source-badge {
      padding: 0.25rem 0.75rem;
      background: var(--china-accent-light);
      border-radius: 9999px;
      font-size: 0.75rem;
    }
  }

  .statistics-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 1rem;
    margin-top: 1rem;
  }
}

.news-item {
  padding: 0.75rem;
  border-left: 2px solid transparent;
  transition: all 0.2s;

  &:hover {
    background: var(--hover-bg);
    border-left-color: var(--china-accent);
  }

  .rank-badge {
    &.rank-hot {
      background: var(--hot-orange);
      color: white;
    }

    &.rank-normal {
      background: var(--gray-200);
    }
  }
}
```

---

## 交互设计

### 1. 折叠/展开动画

```typescript
// 使用 framer-motion
<motion.div
  initial={{ height: 0, opacity: 0 }}
  animate={{ height: "auto", opacity: 1 }}
  exit={{ height: 0, opacity: 0 }}
  transition={{ duration: 0.3 }}
>
  {content}
</motion.div>
```

### 2. 新闻链接点击

```typescript
const handleNewsClick = (url: string, title: string) => {
  // 打开外部链接
  window.open(url, '_blank', 'noopener,noreferrer');

  // 可选：记录分析
  analytics.logEvent('china_news_click', {
    title,
    url,
    timestamp: new Date().toISOString()
  });
};
```

### 3. 刷新数据

```typescript
// 添加刷新按钮
<PixelButton
  icon={<RefreshCw />}
  onClick={refreshChinaNews}
  loading={isRefreshing}
>
  刷新
</PixelButton>
```

---

## 备选设计方案

### 方案B：Tab 标签页式

```
┌─────────────────────────────────────────────────────────────────┐
│  [🔥 热门]  [📡 RSS]  [📰 分组]  [📊 统计]                      │
├─────────────────────────────────────────────────────────────────┤
│                                                                   │
│  (当前标签页内容)                                                 │
│                                                                   │
└─────────────────────────────────────────────────────────────────┘
```

**优点**：切换方便，单屏信息密度低
**缺点**：需要多次点击才能看到全部信息

### 方案C：模态弹窗式

点击"查看今日中文新闻"按钮 → 打开全屏模态窗口

**优点**：不占用 Feed 空间
**缺点**：发现性差，用户可能忽略

---

## 推荐实现顺序

1. ✅ **Phase 1**: 基础框架
   - 创建 `ChinaNewsSection` 组件
   - 实现条件渲染（仅中文）
   - 添加概览卡片

2. ✅ **Phase 2**: 核心功能
   - 实现 Firebase Function 监听
   - 数据获取和缓存
   - 热门新闻 Top 10

3. ✅ **Phase 3**: 扩展功能
   - RSS 科技订阅
   - 更多新闻分组
   - 折叠/展开交互

4. ✅ **Phase 4**: 优化
   - 性能优化（懒加载、虚拟滚动）
   - 动画效果
   - 错误处理

---

## 数据更新策略

```typescript
// 策略1：实时监听（推荐）
useEffect(() => {
  const unsubscribe = onSnapshot(
    doc(db, 'cache', 'latest_china_news'),
    (snapshot) => {
      setData(snapshot.data());
    }
  );

  return () => unsubscribe();
}, []);

// 策略2：定时轮询
useEffect(() => {
  const interval = setInterval(() => {
    refreshData();
  }, 5 * 60 * 1000);  // 每5分钟

  return () => clearInterval(interval);
}, []);
```

---

## 可访问性考虑

```typescript
// 1. 键盘导航
<button
  onClick={toggleExpand}
  aria-expanded={isExpanded}
  aria-label={`展开${title}`}
>

// 2. 屏幕阅读器
<div role="region" aria-label="今日中文新闻速报">

// 3. 语义化标签
<article>
  <header>
  <section>
```

---

## 总结

**推荐设计**：三层折叠式

- ✅ **空间高效**：默认仅 330px
- ✅ **信息完整**：可展开查看所有数据
- ✅ **用户友好**：逐层展开，不会overwhelm
- ✅ **符合风格**：使用现有组件和设计语言
- ✅ **性能优良**：懒加载 + 虚拟滚动

**下一步**：审阅设计后开始实现

---

**文档创建日期**: 2026-01-22
