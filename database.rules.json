{
  "rules": {
    "presence": {
      ".read": "auth != null",
      "$userId": {
        ".write": "$userId === auth.uid"
      }
    },
    "matchmaking_queue": {
      ".read": "auth != null",
      "$userId": {
        ".write": "$userId === auth.uid"
      }
    },
    "active_matches": {
      ".read": "auth != null",
      "$matchId": {
        ".write": "auth != null && (data.child('playerAId').val() === auth.uid || data.child('playerBId').val() === auth.uid || !data.exists())"
      }
    }
  }
}
